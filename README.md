# gulp-file-parser

## parser(parseFunc, options)

## Usage
```js
var parser = require('gulp-file-parser');

var gulp_jeml = fparser(require('jeml').parse, {
    name: 'gulp-jeml',
    extension: '.js'
});
```
